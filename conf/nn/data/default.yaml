defaults:
  - dataset: resisc45
  - _self_

data_path: "${oc.env:PROJECT_ROOT}/data"
eval_datasets:
  - ${nn.data.dataset.dataset_name}

train_dataset: "${nn.data.dataset.dataset_name}Val" # TODO: why val?

ft_epochs:
  DTD: 76
  EuroSAT: 12
  GTSRB: 11
  SUN397: 14
  ImageNet: 6
  STL10: 6

accelerator: ${train.trainer.accelerator}

num_workers:
  train: 4
  val: 2
  test: 0

batch_size:
  train: 128
  val: 128
  test: 16
