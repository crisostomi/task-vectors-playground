accelerator: ${train.trainer.accelerator}
batch_size:
  test: 16
  train: 32
  val: 32
data_path: ${oc.env:PROJECT_ROOT}/data
defaults:
- dataset: cola
- _self_
max_seq_length: 128
num_workers:
  test: 0
  train: 4
  val: 2
splits_pct:
  test: 0.1
  train: 0.8
  val: 0.1
train_dataset: ${nn.data.dataset.dataset_name}
